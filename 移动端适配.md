# LogicFlow移动端适配

## 背景

1. 样式以及布局问题，并且画布不会自适应大小
   - 在移动端中，页面的布局以及画布仍然保持 pc 端设置，导致无法显示完整的内容，让用户感观非常差
   - 画布中的节点发生错位，使得原本节点位置空白
2. 功能以及交互问题：画布、节点和边拖拽时没有反应，插件中涉及到移动的功能也无法生效（拖拽生成节点、小地图等）
3. 节点以及锚点拖拽到画布边缘时，画布不会自动平移（仅当触碰到右边缘时该功能失效）

## 现状-目标

### 现状

1. PC 端依赖 `mousedown`、`mousemove`、`mouseup` 等事件来实现拖拽，而移动端对应的是 `touchstart`、`touchmove`、`touchend`。源码中并没有对移动端事件有所支持，因此导致上述问题。

   //代码如下这文件
   ```text
   packages/core/src/view/node/BaseNode.tsx
   packages/core/src/util/drag.ts
   ```

2. 关于拖拽到画布边缘无法平移的问题，是因为画布没有自适应屏幕的原因。画布宽度与 pc 端一致，但是移动端中展示不全，所以移动端看似拖拽到右侧边缘，但实际离右侧仍有一部分距离，因此无法触发平移逻辑。

### 目标

1. 实现画布自适应，随着屏幕的变化而自动缩放，画布中的节点也同步进行缩放
2. 解决画布中节点错位的问题，不会因为屏幕变化而导致丢失原本位置
3. 实现画布、节点、边通过拖拽进行移动，锚点之间通过拖拽连接
4. 实现插件里小地图可以通过拖拽进行阅览，节点可以通过拖拽生成

## jishufangan

### 统一移动端操作逻辑

- 单指拖动画布
- 单指拖动节点
- 单指拖动锚点
- 双指捏合缩放
- 长按替代右键菜单
- 单击聚焦
- 双击编辑文本

### 输入层的xuanxing

TouchEvent 和 PointerEvent 都是浏览器用于处理用户输入的事件接口，经过调研后选择 PointerEvent。

- TouchEvent 仅用于处理触摸设备的手指交互，是早期为触摸设备设计的事件接口；PointerEvent 是 W3C 推出的新一代指针事件（2019 年正式发布），旨在统一所有“指针输入设备”的事件处理，包括触摸（手指）、光标（鼠标）、触控笔等。
- PointerEvent 比 TouchEvent 多四种事件类型，分别是：`pointerover` / `pointerout`（类似 `mouseover` / `mouseout`）、`pointerenter` / `pointerleave`（类似 `mouseenter` / `mouseleave`）。使得 PointerEvent 可以同时处理手指触摸和鼠标点击事件，不像 TouchEvent 需要额外写一套逻辑对它进行处理。
- 二者核心属性的差异：

  - TouchEvent 事件
    - 该事件中触摸点会打包为列表（`touches`、`targetTouches`、`changeTouches`），每个触摸点是一个 Touch 对象，包含触摸点的坐标（`clientX`、`clientY`）和唯一标识（`identifier`）
    - 涉及到多指操作时，需要通过 identifier 去列表中手动匹配找到对应触摸点，以此跟踪手指的位置变化
    - 由于列表随着手指增减动态变化，所以每次列表变化时都需重新匹配

  - PointerEvent 事件
    - PointerEvent 为每一个指针（手指、鼠标、触控笔等）分配唯一的 PointId，并单独触发事件（每个指针的移动、抬起都会生成独立事件）
    - 涉及多指操作时，只需维护一个 PointMap 用来缓存活跃的指针，通过 id 去缓存中寻找对应的指针，然后获取到坐标，双指间距离无需遍历列表

总体来说，PointerEvent 统一了输入模型，多端只需写一套逻辑，不需要像 TouchEvent 为移动端专门写一套逻辑；涉及多指操作 PointerEvent 处理也更高效，并且事件类型更加丰富；PointerEvent 作为现代化标准，解决了触摸设备和鼠标的割裂问题。

### 改动目标

- 统一输入层为 PointerEvent，不再区分鼠标与触摸路径
- 统一操作函数引用链使用，避免新建功能函数
- 在拖拽时禁止页面滚动，确保移动端可用

### fangansheji

#### 首要：解决hexingongneng在移动端的问题

- 节点（拖拽、尺寸控制、旋转等问题）
  - 已知拖拽的触发在 `util/drag.ts` 中的 `handleMouseDown` 方法：添加拖动和指针释放监听器，找到 `handleMouseMove` 和 `handleMouseUp` 方法；统一事件类型为 PointerEvent，其他逻辑不变，将原先的 Mouse 事件替换为对应的 Pointer 事件
  - 尺寸控制和旋转的解决方式同上：找到对应文件后，对渲染出来的组件修改监听方法，将 `onMouse...` 方法替换为 `onPointer...` 方法
- 边（端点调整、曲线调整、折线调整、锚点拖拽）
  - 找到边类型对应类文件，在其中找到 `onMouseDown` 事件，将事件改为 `onPointerDown`，事件类型为 PointerEvent；功能文件可通过源码文件名确认，改动方式同上
  - 锚点拖拽到画布边缘，画布无法自主平移（仅右侧）的问题
    - 当用户初始位置设置宽度使得画布宽度固定，导致在移动端画布右侧在视图外，进而引发该问题
    - 该问题的解决方案归属到用户：用户根据自身是否需要该功能去修改他的配置

- 画布（双指缩放、拖拽）
  - 解决拖拽问题直接找到 `CanvasOverlay` 文件：将 `mouseDownHandler` 函数中参数的事件类型改为 PointerEvent，监听事件从 `onMouseDown` 改为 `onPointerDown`；渲染出的 svg 额外添加 `touch-action: none` 的样式（该属性是 PointerEvent 规范的一部分），禁用浏览器对触控的默认处理，让设置的指针事件接管交互
  - 在画布层（CanvasOverlay）增加双指缩放：
    - 新增变量用来记录多指状态以及初始数据
      - `pointers`：缓存当前指针坐标（`new Map()` 创建）
      - `pinchDistance`：初始两指距离
      - `pinchStartScale`：初始缩放比例
    - 在处理指针落下的函数中添加额外逻辑
      - 增加 `this.pointers.set(ev.pointerId, { x: ev.clientX, y: ev.clientY })` 将每一个指针放入 pointers 中
      - 当 `pointers.size === 2` 时，计算当前状态，计算出两指距离，初始缩放比例设置为默认值 1，然后调用 `stopDrag.cancelDrag` 防止在缩放时导致画布被拖拽
    - 监听画布层的 `onPointerMove` 事件（只有当 `pointers.size >= 2` 时，才会进入下述逻辑）
      - 两指移动时，实时更新 pointers 中指针坐标，并计算当前两指间距离；通过当前两指间距离 / 初始两指间距离得到当前缩放比例
      - 以两指中点作为缩放原点，通过 `graphModel.getPointByClient` 得到中点对应的 canvas 坐标
      - 将中点对应的画布层坐标以及当前缩放比例传入 `transformModel.zoom` 中，更新 `SCALE_X` / `SCALE_Y`，然后修正 x、y 方向的平移距离来抵消比例变化带来的位移
    - 当手指抬起时，在 pointers 中删除对应指针，并把两指距离和缩放比例设为 undefined
  - 在画布层增加双指移动：
    - 新增变量：
      - `pinchLastCenterX`：前一个双指中点的 x 坐标，用来与当前中点坐标进行对比，进而决定平移画布的距离
      - `pinchLastCenterY`：前一个双指中点的 y 坐标
    - 由于双指移动不同于单指移动，需要选取一个点来移动画布，因此采用中点（与双指缩放选点一样，保证二者功能平滑实现）
      - 在双指落下时，将此时指针中点的 x、y 坐标赋值给两个变量，保证下一帧指针开始移动时，画布平移量基于上一帧中点与当前中点相减得出
        - `deltaX = this.pinchLastCenterX === undefined ? 0 : cx - this.pinchLastCenterX`
      - 在双指移动时，每次计算完平移量后，将 `pinchLastCenterX/Y` 更新为当前指针中点坐标
    - 当手指抬起时，清空上一帧的双指中点（`pinchLastCenterX/Y`）坐标

#### 解决插件中常用功能在移动端的问题

- 实现 MiniMap 可以拖动
  - 找到 miniMap 功能在哪个文件中实现的，然后将原先的 MouseEvent 事件替换为 PointerEvent 事件；不过 click 在 PointerEvent 中没有完全对应的事件，所以 click 保持原状
- 实现长按展示节点、边、画布菜单
  - 在监听指针按下（`mouseDownHandler` / `handleMouseDown`）事件的函数中添加计时器来检测长按事件
    - 初始定义 `LongPressTimer`：当指针按下时先清空计时器，再通过 `setTimeout` 施加 500ms 延迟模拟长按触发；当在 500ms 内指针抬起则再次清空计时器，保证计时器正常触发
    - 移动端和 PC 端展示菜单的逻辑没有区别，所以直接在 `setTimeout` 中将 `handleContextMenu` 作为回调函数传入；当按下指针到达 500ms 且没有进入拖拽状态时，就会展示节点菜单
- 实现拖拽创建节点
  - 由于浏览器对 PointerEvent 存在隐式捕获（从面板中点击、拖拽到画布中时，浏览器会把事件派送至面板层，而不是画布），所以采用全局监听 Pointer 事件
    - 在 dnd 文件中开始拖拽时添加全局监听 `PointerMove` / `PointerUp` 事件，直接调用已有的功能函数触发对应逻辑
    - 在 `pointermove` 中判断指针是否在画布层中：在内则调用 `dragEnter`（首次进入画布时执行，可用 `fakeNode` 作为是否第一次进入的标识）和 `dragover` 方法；在外调用 `dragLeave`
    - 在 `pointerup` 中判定指针是否在画布层中：在画布内则调用 `onDrop` 生成节点，不在画布内则调用 `dragLeave` 清理；不管在不在画布层都要执行 `stopDrag`（移除全局监听的 `PointerMove` / `PointerUp` 事件，保证只有在拖拽生成节点时全局监听存在）
- 实现框选功能（包括独占模式）
  - 主要改动依旧是将监听的 move 和 up 事件转换成 PointerEvent 类型

PS：PointerEvent 继承自 MouseEvent，因此 PointerEvent 监听的事件比 MouseEvent 更丰富；把事件类型为 PointerEvent 的 `e` 传入一个 `(e: MouseEvent)` 的函数中，可以通过 TS 检查。
