# 基于LineEdge的可操作边

### 背景：

https://github.com/didi/LogicFlow/issues/206

用户需要通过边上的控制点自主的操控边的形状，但目前没有一种边类型满足这种需求

### 现状-目标：

#### 现状：

![现状图](./image(1).png)

由于没有一种边的类型满足这种需求，所以我们需要创建一种新的边类型，可操作边(OperableEdge)

#### 目标：

1.在边中间添加控制点，实现可以通过拖动控制点改变边
2.通过快捷键进入编辑态时，渲染出新的控制点，新的控制点的位置是**拖拽后**的控制点的坐标与startPoint/endPoint之间的中点

![目标示意图](./image(2).png)

### 技术方案：

#### 技术选型：

这种新的边类型目前是要基于LineEdge的基础上进行创造，至于为什么不选择基于贝塞尔曲线或者折线的原因在于：
1.贝塞尔曲线/折线都有自己固定的路径算法逻辑，无法满足用户通过拖拽控制点来自定义边的需求
2.直线基本无改动，在直线的基础上重写有较好的重塑性

#### 方案设计：

为实现完全按照自己的拖动来控制边的路径,会创建一个pointList数组,存入边的properties中。

*   **初始化状态**

    初始化时，pointList中只有startPoint和endPoint，因此会通过pointList的长度来判断是否为初始态，只有拖动控制点后，才将控制点推入pointList中，此时长度>2标志着边不再为初始态

*   **首先在边中渲染控制点**

    *   设置变量draggingMode: 'insert' | 'move'来区分控制点是否被拖动过，当是insert的时候代表着该控制点只是在边上渲染出来，并不在pointList中。mode代表着他是被拖动过的控制点，已经被推到pointList中。
    *   因为有两种类型的控制点，所以在获取的时候会遍历两种不同的数组来生成控制点。具体见getControlPoints()方法

*   **拖动控制点实现边路径随之变化**

    *   首先实现控制点可以被拖动，当按下控制点时，触发 this.controlDrag.handleMouseDown(e)方法，controlDrag则是通过new StepDrag生成的，将stepDrag中的onDragging替换为下方的onControlDragging方法，实时更新被拖拽点的位置。具体见onControlDragging方法

    *   实现边路径变化，通过获取当前的points，然后实时更改路径。具体见getPath()方法

    *   getEffectivePoints方法获取的是当前操作态的控制点数组，因为insert类型的控制点是不存在于PointList数组中，所以该方法的处理是，判断当前拖拽的控制点类型，如果是insert类型，会在返回的points数组中添加该控制点的信息（相当于一个临时的pointList）具体见getEffectivePoints()方法


*   **拖动结束后，生成新的控制点**

    *   会先判断刚刚拖拽的控制点类型，如果是insert类型，则会在pointList中插入该控制点的信息，如果为move类型，则会直接更新pointList的信息。具体见getEffectivePoints()方法
*   **交互时边轮廓不会随着边的变化而该改变**

    *   实现下图所示效果，当开始拖拽时，保留一刻边的path，在拖拽时，实际渲染两条边，一条是正在拖拽中的，一条是拖拽前的路径（通过model.draging控制）

    ![拖拽交互示意图](./image(3).png)